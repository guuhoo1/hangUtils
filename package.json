{
  "name": "@wuguohang/hangutils",
  "version": "1.0.6",
  "description": "基于ramda扩展的个人工具库",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "publish:project": "npx tsc && npm version patch && npm publish --access public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ramda",
    "hangutils"
  ],
  "author": "guohang",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@types/ramda": "^0.30.2",
    "if-logger": "^0.12.5",
    "ramda": "^0.30.1"
  }
}
